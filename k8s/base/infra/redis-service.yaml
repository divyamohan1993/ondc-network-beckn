apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: ondc-infra
  labels:
    app: redis
    app.kubernetes.io/part-of: ondc
spec:
  clusterIP: None
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
