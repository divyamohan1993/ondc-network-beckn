apiVersion: v1
kind: Secret
metadata:
  name: admin-secrets
  namespace: ondc
  labels:
    app.kubernetes.io/part-of: ondc
type: Opaque
# REQUIRED: Generate all values before deploy.
# Generate with: openssl rand -base64 32
stringData:
  ADMIN_PASSWORD: ""      # REQUIRED: openssl rand -base64 32
  NEXTAUTH_SECRET: ""     # REQUIRED: openssl rand -base64 32
