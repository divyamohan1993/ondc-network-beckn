apiVersion: v1
kind: Secret
metadata:
  name: infra-secrets
  namespace: ondc-infra
  labels:
    app.kubernetes.io/part-of: ondc
type: Opaque
# REQUIRED: Generate all values before deploy.
# Generate with: openssl rand -base64 32
stringData:
  POSTGRES_PASSWORD: ""   # REQUIRED: openssl rand -base64 32
  REDIS_PASSWORD: ""      # REQUIRED: openssl rand -base64 32
  RABBITMQ_PASSWORD: ""   # REQUIRED: openssl rand -base64 32
